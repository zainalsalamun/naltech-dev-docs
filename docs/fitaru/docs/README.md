# Fitaru Docs

Documentation is grouped by area:

- `product/` for product definition and user flow
- `design/` for brand, UI, and CMS design
- `api/` for database and API contracts
- `planning/` for backlog and architecture decisions
- `mockups/` for static HTML previews and screenshots

